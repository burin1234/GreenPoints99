<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.icenmind.greenpoints">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Index" />
        <activity android:name=".Indexmenu" />
        <activity android:name=".Park" />
        <activity android:name=".history_exchange_garbage" />
        <activity android:name=".history_usepoint" />
        <activity android:name=".menuother" />
        <activity android:name=".Login" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".preview_park_Activity" />
        <activity android:name=".layout_rate_garbage" />
        <activity android:name=".Edit_profile" />
        <activity android:name=".layout_history_exchange_garbage" />
        <activity android:name=".layout_goods_and_services"></activity>
    </application>

</manifest>